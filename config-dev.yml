server:
  port: 3300
spring:
  application:
    name: cloud-config-center3333
  cloud:
    config:
      server:
        git:
          uri: git@github.com:dcitsliyjbb/sprincloud-config.git
          search-paths:
            - springcloud-config
      label: master
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7001/eureka

